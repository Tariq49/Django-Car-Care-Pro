from django.db import models

# Create your models here.

# category ('two wheeler with gear','two wheeler without gear,'three wheeler','four wheeler')
# vehicle_no
# vehicle_name
# vehicle_model
# vehicle_brand
# problem_description
# date of request
# service needed (foriegkey ServiceCostDetail)
#customer=models.ForeignKey('Customer', on_delete=models.CASCADE,null=True)
#mechanic=models.ForeignKey('Mechanic',on_delete=models.CASCADE,null=True)

